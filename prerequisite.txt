# Prerequisite: Install node.js: http://nodejs.org/
# Use nvm if you already have it installed

mkdir ~/.npmprefix
npm config set prefix ~/.npmprefix

# Mac
echo “export PATH=`npm config get prefix`/bin:$PATH” >> ~/.bash_profile
source ~/. bash_profile

# Linux
echo “export PATH=`npm config get prefix`/bin:$PATH” >> ~/.bashrc
source ~/. bashrc

# Mac/Linux
curl https://raw.githubusercontent.com/creationix/nvm/v0.13.1/install.sh | bash
nvm install v0.10

# Windows
# With node installed
npm install -g n
n stable

// Request to join github.com/noderiety